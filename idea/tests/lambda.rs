mod utils;
use mini_lang::types::{Type, TypeError};

use utils::basic_test;

#[test]
fn basic_lambda() {
//    basic_test("
//let apply = fn(f) { fn(x) { f(x) } };
//let add2 = fn(y) { y + 2 };
//let another = apply(add2);
//    ".trim(), vec![])
//    //let add2 = fn(y) { y + 2 };
//    //let result = apply(f)(3);
}

